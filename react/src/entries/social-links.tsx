import { mountComponent } from '../utils/mountComponent';
import { SocialLinks } from "@components/SocialLinks";

/**
 * Social Links Entry Point
 * Hydrates social media icons with tracking
 */
mountComponent('social-links', <SocialLinks />);

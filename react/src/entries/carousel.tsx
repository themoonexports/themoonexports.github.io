import { mountComponent } from '../utils/mountComponent';
import { Carousel } from "@components/Carousel";

/**
 * Carousel Entry Point
 * Hydrates hero image slider
 */
mountComponent('carousel', <Carousel />);
